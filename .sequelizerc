// .sequelizerc

require("babel-register");
const path = require("path");

module.exports = {
  config: path.resolve("src", "configs", "database.cjs"),
  "migrations-path": path.resolve("src", "migrations"),
};
